{
  "name": "melodymatch",
  "version": "1.0.0",
  "scripts": {
    "start": "node server/server.js",  
    "heroku-postbuild": "npm install --prefix server && cd client && npm install && npm run build"
  },
  "engines": {
    "node": "18.x"
  }
}
  